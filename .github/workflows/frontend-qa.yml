name: Frontend QA

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  frontend-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Frontend layout tests
        run: npm run test:frontend-layout

      - name: Frontend runtime QA
        run: timeout 10m npm run qa:runtime
        continue-on-error: true

      - name: Frontend stress QA
        run: timeout 8m npm run qa:stress-runtime
        continue-on-error: true

      - name: Upload QA reports
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: frontend-qa-reports
          path: |
            reports/runtime-ux-qa.json
            reports/runtime-ux-stress.json
          if-no-files-found: warn
